[variables]
# Default values for build phase - will be overridden by Railway at runtime
DATABASE_TYPE = "sqlite"

[phases.setup]
nixPkgs = ["nodejs-22_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start:prod"
