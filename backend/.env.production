# Railway Production Environment
# These are template values - Railway will use the actual values from Variables

DATABASE_TYPE=postgres
NODE_ENV=production
PORT=3001

# PostgreSQL connection (Railway injects these automatically)
# DATABASE_URL is provided by Railway when you reference Postgres service
